#pragma once

/** Default / design-resolution logical size (viewport may inset via web layout). */
static constexpr int   SCREEN_W = 900;
static constexpr int   SCREEN_H = 700;
static constexpr float PI       = 3.14159265f;
