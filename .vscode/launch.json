{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "asteroids (WEBVIEW_INSPECTABLE)",
      "type": "cppdbg",
      "request": "launch",
      "preLaunchTask": "make build",
      "program": "${workspaceFolder}/build/asteroids",
      "args": [],
      "cwd": "${workspaceFolder}",
      "environment": [
        {
          "name": "WEBVIEW_INSPECTABLE",
          "value": "1"
        }
      ],
      "MIMode": "lldb"
    },
    {
      "name": "asteroids (WEBVIEW_INSPECTABLE, CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "make build",
      "program": "${workspaceFolder}/build/asteroids",
      "cwd": "${workspaceFolder}",
      "env": {
        "WEBVIEW_INSPECTABLE": "1"
      }
    }
  ]
}
