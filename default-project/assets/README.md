# Project assets

Sample PNGs here (`*.png`) exercise `draw.sprite` and the Sprite behavior (`prop.asset` in the inspector). They are small procedurally generated images checked into the repo so CI and offline builds do not depend on external downloads.

For CC0 artwork to drop in, see [OpenGameArt.org](https://opengameart.org/) (e.g. space shooters, pixel ships).
